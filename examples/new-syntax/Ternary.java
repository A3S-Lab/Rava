class Ternary {
    public static void main(String[] args) {
        int x = 10;
        String result = x > 5 ? "big" : "small";
        System.out.println(result);
        int y = 3;
        String result2 = y > 5 ? "big" : "small";
        System.out.println(result2);
    }
}
